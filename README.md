# godiddy-cli

A command-line interface for Godiddy.

## Build

```shell
mvn clean package
```

## Run

Get command line

```shell
java -jar target/cli-*-jar-with-dependencies.jar -h
```

Set endpoint

